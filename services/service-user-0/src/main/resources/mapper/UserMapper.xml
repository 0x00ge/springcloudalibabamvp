<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mvp.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.mvp.user.entity.User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="gender" column="gender"/>
        <result property="birthday" column="birthday"/>
        <result property="permission" column="permission"/>
        <result property="status" column="status"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, phone, email, password_hash, name, avatar_url,
        gender, birthday, permission, status, last_login_at, created_at,
        updated_at, deleted_at
    </sql>




    <!-- resultType：按字段名自动映射（下划线转驼峰需开启 map-underscore-to-camel-case） -->
    <select id="userList" resultType="com.mvp.user.entity.User">
        select
        <include refid="Base_Column_List"/>
        from t_user
    </select>

    <!-- resultMap：使用 BaseResultMap 做列 → 属性映射；列片段只能 include Base_Column_List -->
    <select id="userMapList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user
    </select>

    <!-- if + where：可选条件，where 自动处理 WHERE / 去掉多余 AND -->
    <select id="findUsers" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user
        <where>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!-- choose / when / otherwise：多分支，类似 if-else if-else -->
    <select id="findByKeyword" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user
        <where>
            <choose><!-- switch -->
                <when test=" phone != null and phone != '' "><!-- case -->
                    phone = #{phone}
                </when>
                <when test=" name != null and name != '' ">
                    name = #{name}
                </when>
                <otherwise>
                    1 = 1
                </otherwise>
            </choose>
        </where>
    </select>

    <!-- set + if：动态 UPDATE，set 自动处理逗号 -->
    <update id="updateUser">
        update t_user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = now()
        </set>
        where id = #{id}
    </update>

    <!-- foreach：IN 查询 / 批量场景 -->
    <select id="listByIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- bind：先算变量再拼进 SQL（常用于 like） -->
    <select id="searchByName" resultMap="BaseResultMap">
        <bind name="nameLike" value="'%' + name + '%'"/>
        select
        <include refid="Base_Column_List"/>
        from t_user
        where name like #{nameLike}
    </select>

    <!-- 综合：include + where + if + ${} 排序 + limit（orderBy 必须白名单校验，防注入） -->
    <select id="userListPage" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            and deleted_at is null
        </where>
        <if test="orderBy != null and orderBy != ''">
            order by ${orderBy}
        </if>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

</mapper>
