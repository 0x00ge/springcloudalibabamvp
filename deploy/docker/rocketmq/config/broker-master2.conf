# broker-group2 主节点（brokerId=0）
# 与 broker-group1 并行承载流量，两组 master 共同分担 Producer 写入压力

# ── 集群 / 节点身份 ────────────────────────────────────────────────────────────
brokerClusterName=DefaultCluster
brokerName=broker-group2
brokerId=0
brokerRole=ASYNC_MASTER
flushDiskType=ASYNC_FLUSH

# ── 网络 ──────────────────────────────────────────────────────────────────────
namesrvAddr=namesrv-1:9876;namesrv-2:9876
brokerIP1=broker-master2
# 端口与 broker-group1 的 master1（10911/10912）错开，避免宿主机端口冲突
listenPort=10931
haListenPort=10932

# ── Topic / SubscriptionGroup 自动创建 ───────────────────────────────────────
autoCreateTopicEnable=true
autoCreateSubscriptionGroup=true

# ── 消息存储 ──────────────────────────────────────────────────────────────────
deleteWhen=04
fileReservedTime=48
storePathRootDir=/home/rocketmq/store
storePathCommitLog=/home/rocketmq/store/commitlog
