services:
  # Nginx 作为本地 gateway 集群统一入口。
  # 前端访问 127.0.0.1:8000，Nginx 再转发到本机 8001/8002 两个 gateway 实例。
  nginx-gateway:
    image: nginx:1.30-alpine
    container_name: nginx-gateway
    ports:
      - "8000:8000"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
